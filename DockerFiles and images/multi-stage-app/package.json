{
  "name": "ana-multistage-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Task 1 - one app, two Dockerfiles, so the size difference is measurable",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "node build.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "esbuild": "^0.23.0"
  }
}
